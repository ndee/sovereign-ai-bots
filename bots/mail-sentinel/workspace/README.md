# Mail Sentinel workspace

Provisioned by sovereign-node install flow.
Managed by Sovereign Node installer.

- `config/default-rules.json` seeds the local relevance rules.
- `data/` stores local Mail Sentinel state at runtime.
