# Wealth Alignment data

- `wealth-alignment-state.json` is the local state file: documents, accounts, transactions, assets, liabilities, monthly snapshots, and counters.
- This directory is local state. It is not package-managed after first install.
- Treat the contents as private financial data.
