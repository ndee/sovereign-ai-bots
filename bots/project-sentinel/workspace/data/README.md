# Project Sentinel data

- `project-sentinel-state.json` stores scan timestamps, dedupe history, delivered signals, digest queue state, and feedback history.
- This directory is local state. It is not package-managed after first install.
