# node-operator-core

Provisioning metadata for the deterministic Node Operator daemon. Command
parsing, authorization, and dispatch are implemented in
`bin/node-operator.js` — not by any model. See AGENTS.md for the command
grammar and the authorization rules.
