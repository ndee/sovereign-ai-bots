# sovereign-ai-bots

Installable Sovereign bot packages consumed by `sovereign-node`.

Each package lives under `bots/<id>/` and contains:

- `sovereign-bot.json`: package manifest
- `workspace/`: files copied into the managed bot workspace

Current packages:

- `bitcoin-skill-match`
- `mail-sentinel`
- `node-operator`
